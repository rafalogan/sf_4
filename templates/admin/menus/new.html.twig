{% extends'admin/admin.html.twig' %}

{% block body %}
    <h1> Novo menu </h1>
    <hr>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit"> Salvar </button>
    {{ form_end(form) }}
{% endblock %}